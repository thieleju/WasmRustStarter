{
  "name": "mandelbr0t",
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build:rust": "wasm-pack build --target web --out-name wasm --out-dir ./pkg",
    "build:js": "vite build",
    "build": "npm run build:rust && npm run build:js"
  },
  "dependencies": {
    "vite": "5.1.4"
  }
}
